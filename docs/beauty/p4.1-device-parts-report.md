# P4.1 — pedido de FaceParts no jaw (dispositivo)

Baseline intacto. **`semanticReleasedFill=false`.** Sem P5, sem núcleo V2, sem PNGs extra do P4.

## O que mudou

Jaw/chin/v_face no ROI passam a **pedir** `FacePartsDetector` quando `silhouetteRequestsFaceParts` está on (default `kDebugMode`). Rollback: `FaceWarpV3Config.silhouetteRequestsFaceParts = false`.

A máscara 6 classes **não** entra no `ExtendedRoiPipeline.compose` / `ContourSourceClassifier`. Geometric **não** é guardado como `lastFaceParsing` neste caminho. O RGBA do jaw 50% permanece o P0.

`source=mappedMulticlass` só é aceite se `FacePartsSegmentation` nativa não estiver vazia e o mapper não cair em geometric.

## Medição p01 / p05 / p12 jaw 50%

Ambiente deste relatório: `flutter test` macOS. `FacePartsDetectorStub` → **blocked**. Cobertura nativa = vazia. Ear = 0.

| Fixture | Hash (flag off, parsing não injectado) | mapped | ear |
|---|---|---|---|
| p01 | `5580e606c837f9e2` | blocked | 0 |
| p05 | `2d26e8a3dc1d7807` | blocked | 0 |
| p12 | `5a74ce412270b2df` | blocked | 0 |

`amplitudeScale=0.5`, renderer A. Contrato mapper (sintético, rotulado): hair / bodySkin→neck / clothes / faceSkin presentes; `earL`/`earR` = 0; `allowEar=false`.

No Android/iOS, após **full restart** do `flutter run`, o log deve mostrar `roiPartsRequested=true` e `roiPartsStatus=present` se o `selfie_multiclass_256x256` carregar, ou `blocked` se `detectFaceParts` voltar null. As classes a ler são `background, hair, bodySkin, faceSkin, clothes, others`. Ear continua 0 até existir parser 19 real.

JSON: `.cursor/extended-roi/p4.1/summary.json`.

## Não feito

- Fill semântico continua off
- Parsing 6 classes não foi injectado no warp
- BiSeNet continua ausente
- personTransport / composer / Phase9Local / inpaint experimental / JawPersonBoundary / faceSlimUsesRoi / B/C / Telea agressivo não reactivados
- Amplitude não subiu
- `FacialWarpCoreV2` não iniciado

Aguardar cobertura nativa no dispositivo (`roiPartsStatus=present`) antes de qualquer V2.
